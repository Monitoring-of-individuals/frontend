import React, { FC } from 'react';
import './AuthorisedUserPage.css';

import RequestForm from '../../components/RequestForm/RequestForm';

const AuthorisedUserPage: FC = (): React.ReactElement => {
  return <RequestForm />;
};

export default AuthorisedUserPage;
